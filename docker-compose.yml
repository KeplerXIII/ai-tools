services:
  ai-tools:
    build: .
    container_name: ai-tools
    env_file:
      - .env
    ports:
      - "8010:8000"
    networks:
      - ai_stack
    shm_size: "1gb"
    restart: unless-stopped

networks:
  ai_stack:
    external: true